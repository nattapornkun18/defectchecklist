# Prompt สำเร็จรูป — เอาไว้สั่งงานรอบต่อไป

ไฟล์นี้เก็บ prompt ที่ก๊อปไปวางได้เลยตอนเริ่ม session ใหม่
เพราะ AI จะจำ session เก่าไม่ได้ ต้องบอกบริบทให้ครบทุกครั้ง

---

## 1) เพิ่ม room type ใหม่ (โรงแรมเดิม)

> ใช้ตอนมีฟอร์มของ room type อื่น เช่น deluxe king - left, grand corner, deluxe twin
> ผลตรวจทุก type จะไหลเข้า Google Sheet เดียวกัน และหน้าสรุปจะแยกกลุ่มให้เอง

**สิ่งที่ต้องแนบ:** PDF ของ room type นั้น แยกไฟล์ต่อ 1 หมวด (ไฟล์ละหมวด ตั้งชื่อไฟล์ตามชื่อหมวด)
แต่ละไฟล์ขอ **รูปที่มีหมายเลขจุดตรวจกำกับ** ส่วนตารางรายการจะอยู่ในไฟล์เดียวกันหรือส่งแยกก็ได้

```
repo: nattapornkun18/defectchecklist
branch: claude/defect-inspection-html-form-hacy4z

ตอนนี้ระบบรองรับ room type "10 - Deluxe King - Right" อยู่แล้ว (488 จุดตรวจ 10 หมวด)
อ่าน README.md กับ js/checklist.js ให้เข้าใจโครงสร้างก่อน

ขอเพิ่ม room type ใหม่: <ชื่อ type> สำหรับห้อง <เลขห้อง เช่น 03, 05, 09>
ไฟล์ที่แนบมาคือรูปอ้างอิงแยกรายหมวด

ทำให้หน่อย:
1. ถอดรายการจุดตรวจจากตารางให้ครบ (ระวังฟอนต์ไทยที่เก็บวรรณยุกต์ไว้ใน Private Use Area
   ต้อง map กลับเป็น Unicode และตรวจทานกับภาพหน้าเอกสาร ห้ามเดา)
2. ใส่รูปลง img/ โดยไม่บีบอัดซ้ำ (เคยลองแล้วมันทำลายความคมของตัวเลข)
3. ทำเป็นหน้าแยกอีกลิงก์ ชี้ Web App URL เดิม เพราะคอลัมน์ RoomType แยกให้อยู่แล้ว
4. บวก ASSET_VERSION ให้ครบทั้ง 4 ที่ตาม README
5. เทสต์ด้วย Playwright แล้วรายงานผลจริงว่าผ่านอะไรบ้าง
```

---

## 2) ทำระบบใหม่ให้โรงแรมอื่น

> ใช้ตอนขึ้นโรงแรมใหม่ทั้งชุด — Sheet คนละไฟล์ Apps Script คนละตัว

**สิ่งที่ต้องแนบ:** PDF ฟอร์มตรวจแยกรายหมวด (เหมือนข้อ 1) ของ room type แรกที่จะทำ

```
repo: <repo ใหม่ของโรงแรมนี้>
branch: <branch ที่จะให้ทำงาน>

อยากได้ระบบตรวจ defect ห้องพักแบบเว็บ ใช้แทนการปริ้นฟอร์มสี
เอาแบบเดียวกับที่เคยทำให้ที่ repo nattapornkun18/defectchecklist
(ถ้าเข้าถึงได้ให้ไปดูโครงสร้างก่อน ถ้าไม่ได้จะอธิบายให้)

สเปกที่ต้องการ:
- static HTML ล้วน ไม่ต้อง build ไม่ต้องมี dependency วางบน GitHub Pages ได้เลย
- หน้าแรกเป็นการ์ดรายหมวด กดเข้าไปเจอรูปอ้างอิงที่มีเลขจุดตรวจ + รายการให้กดผล
- ผลตรวจ 3 แบบ: ผ่าน / ส่งคืน / แก้เอง — กรอกเฉพาะที่มีปัญหา ที่เหลือเป็นผ่านอัตโนมัติตอนกดบันทึก
- ถ้าเป็น ส่งคืน/แก้เอง ให้ใส่จำนวนจุด รายละเอียด และถ่ายรูปแนบได้
- ตัวดูรูปซูมด้วยสองนิ้วได้ + ช่องพิมพ์เลขข้อเพื่อกระโดดไปที่ข้อนั้น
- เซฟลง localStorage อัตโนมัติ แยกร่างตามห้อง ตรวจตอนไม่มีเน็ตได้ มีคิวส่งภายหลัง
- บันทึกลง Google Sheet ผ่าน Apps Script Web App บันทึกซ้ำต้องแทนที่ ไม่ใช่เพิ่มแถวซ้ำ
- ดึงข้อมูลจาก Sheet กลับมาแก้ต่อได้ (two-way)
- หน้าสรุปรวมทุกห้อง แยกกลุ่มตาม room type กดที่ตัวเลขแล้วดูรายจุดที่ต้องแก้ได้
- มีระบบเช็คเวอร์ชัน เวลาอัปเดตแล้วผู้ใช้เห็นแถบ "มีเวอร์ชันใหม่" ให้กด
- UI ภาษาไทย เขียน README ภาษาไทยให้ด้วย

ไฟล์ที่แนบคือฟอร์มตรวจของ room type แรก ทำให้ครบแล้วเทสต์ด้วย Playwright
รายงานผลจริงว่าผ่านอะไรบ้าง อย่าบอกว่าเสร็จถ้ายังไม่ได้ทดสอบ
```

---

## 3) แก้เนื้อหาเล็กน้อย (ข้อความ / รูป / รายชื่อห้อง)

```
repo: nattapornkun18/defectchecklist
branch: claude/defect-inspection-html-form-hacy4z

<บอกสิ่งที่จะแก้ เช่น "แก้ข้อความหมวด Door ข้อ 16 จาก ... เป็น ..."
หรือ "เปลี่ยนรูปหมวด Floor" พร้อมแนบไฟล์>

อย่าลืมบวก ASSET_VERSION ให้ครบทั้ง 4 ที่ตาม README ด้วย
```

---

## เรื่องที่ต้องบอกทุกครั้ง (ไม่งั้นจะพลาดซ้ำ)

รวมบทเรียนจากรอบที่ผ่านมา เอาไปแปะเสริมได้ถ้า AI ทำพลาด:

| เรื่อง | สิ่งที่ต้องระวัง |
|---|---|
| **ฟอนต์ไทยใน PDF** | วรรณยุกต์บางตัวถูกเก็บใน Private Use Area (U+F70x/U+F71x) ถอดตรง ๆ จะได้ "เป?ด" ต้อง map กลับและตรวจทานกับภาพ |
| **PDF รวมหลายหน้าทำสีเพี้ยน** | ถ้า export ทั้งไฟล์เป็น PDF เดียว รูปจะซีดและสีชมพูหาย ต้อง **export แยกรายหมวด** |
| **ห้ามบีบอัดรูปซ้ำ** | re-encode ที่คุณภาพ 0.88 ทำให้ 217 KB เหลือ 25 KB และตัวเลขกำกับจุดตรวจเบลอ ให้ก๊อปไฟล์ต้นฉบับเข้าไปตรง ๆ |
| **แคชเบราว์เซอร์** | ต้องบวกเลขเวอร์ชัน 4 ที่ (`js/config.js`, `version.json`, และ `?v=` ใน 3 ไฟล์ HTML) ไม่งั้นมือถือจะเห็นของเก่า |
| **Apps Script deploy** | แก้ `Code.gs` แล้วต้อง **Manage deployments → ✏️ → New version → Deploy** ถ้ากด New deployment ใหม่ URL จะเปลี่ยน ต้องไปแก้ `config.js` ตาม |
| **Who has access** | ต้องเป็น `Anyone` เท่านั้น ถ้าเป็น "Anyone with Google account" หน้าเว็บจะส่งข้อมูลไม่ได้ |

---

## ข้อมูลระบบปัจจุบัน (เอาไว้อ้างอิง)

```
repo        nattapornkun18/defectchecklist
branch      claude/defect-inspection-html-form-hacy4z
เว็บ         https://nattapornkun18.github.io/defectchecklist/
room type   10 - Deluxe King - Right
ห้อง         310, 410, … 2010 (18 ห้อง)
จุดตรวจ      488 จุด ใน 10 หมวด
Sheet       Inspections (สรุป) + DefectLog (รายจุด) + สรุปรวม (ตารางสรุปอัตโนมัติ)
```

---

## สรุปสั้น ๆ สำหรับนำเสนอ

**ปัญหาเดิม:** ตรวจ 1 ห้องต้องปริ้นเอกสารสี 1 ชุด เปลืองและช้า เก็บเป็นไฟล์ออนไลน์ไม่ได้
สรุปยอดต้องมานั่งกรอกตัวเลขในชีทเองทีละช่อง

**ที่ทำได้ตอนนี้:**

- เปิดบนมือถือ ไม่ต้องปริ้นเลย — รูปอ้างอิงพร้อมเลขจุดตรวจอยู่ในเว็บ ซูมดูได้
- **กรอกเฉพาะจุดที่มีปัญหา** ที่เหลือระบบตั้งเป็น "ผ่าน" ให้เอง จาก 488 จุดอาจแตะแค่ 20–30 จุด
- แนบรูปถ่าย defect ได้ ขึ้น Google Drive อัตโนมัติ
- ตรวจตอนไม่มีสัญญาณได้ กลับมามีเน็ตแล้วส่งเอง
- ข้อมูลเข้า Google Sheet ทันที ทำ Pivot ต่อได้ ไม่ต้องกรอกซ้ำ
- **หน้าสรุปรวมทุกห้อง** แทนตารางที่เคยกรอกมือ กดที่ตัวเลขดูได้เลยว่าจุดไหนต้องส่งแก้ พิมพ์เป็นใบสั่งงานส่งผู้รับเหมาได้
- **ตารางสรุปโผล่ในชีทเองด้วย** (แท็บ "สรุปรวม") สร้างใหม่อัตโนมัติทุกครั้งที่มีคนบันทึก ไม่ต้องกรอกมืออีก
- ตรวจซ้ำหลายรอบได้ แยกรอบ แยกวันที่ ไม่ทับกัน
- ต่อยอดไปโรงแรมอื่นได้ ใช้โครงเดิม เปลี่ยนแค่เนื้อหาฟอร์มกับรูป

**ต้นทุน:** ฟรีทั้งหมด — GitHub Pages + Google Sheet + Apps Script ไม่มีค่าเซิร์ฟเวอร์ ไม่มีค่า license
